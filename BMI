#include<stdio.h>

int main(void){
	float weight,height,bmi;
  printf("Insert weight(kg) = ");
  scanf("%f",&weight);
  printf("Insert height(cm) = ");
  scanf("%f",&height);
  bmi = weight / (height*height);
  printf("%f",bmi);
	return 0;
}
